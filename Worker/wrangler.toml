name = "stego-signaling-test"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Durable Objects let each "room" hold its own WebSocket connections
[[durable_objects.bindings]]
name = "ROOMS"
class_name = "SignalingRoom"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["SignalingRoom"]